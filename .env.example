# Exemplo de variáveis de ambiente
# Configure estas variáveis no seu ambiente de deployment

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Admin Authentication (em produção, implementar com backend seguro)
VITE_ADMIN_CREDENTIALS=your_encrypted_admin_credentials

# Instruções para desenvolvimento local:
# 1. Copie este arquivo para .env
# 2. Substitua os valores pelos dados reais do seu projeto Supabase
# 3. Para teste local, use: admin@uniagro.com / dev_admin_2025
# 4. NUNCA faça commit do arquivo .env no repositório

# Instruções para Netlify:
# 1. Vá para o painel do Netlify
# 2. Acesse Site settings > Environment variables  
# 3. Adicione as variáveis acima com os valores reais do seu projeto Supabase

# IMPORTANTE: 
# - Use apenas valores de exemplo neste arquivo
# - Mantenha as credenciais reais fora do controle de versão
# - Em produção, implemente autenticação com hash seguro no backend
